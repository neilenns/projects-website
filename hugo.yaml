# Hugo configuration file
title: Neil's projects

# Required so display of last modified date on pages works
enableGitInfo: true

# import hextra as module
module:
  imports:
    - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: Documentation
      pageRef: /docs
      weight: 1
    - name: Search
      weight: 2
      params:
        type: search
    - name: GitHub
      weight: 3
      url: "https://github.com/neilenns"
      params:
        icon: github

params:
  displayUpdatedDate: true

  navbar:
    displayTitle: true
    displayLogo: false

  footer:
    displayCopyright: true
    displayPoweredBy: false

  editURL:
    enable: false
    base: "https://github.com/neilenns/projects-website/edit/main/content"

  versions:
    trackaudio: "v4.0.0"
    vATIS: "v1.1.1"
